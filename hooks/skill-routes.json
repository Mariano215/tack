{
  "enabled": true,
  "comment": "Skill router table. First match wins; order = priority. Patterns are case-insensitive regex. Hints are advisory and cost ~40 tokens only on match.",
  "routes": [
    {
      "id": "pre-push",
      "pattern": "(ready to push|can i push|safe to push|before (i|we) push|pre[- ]?push|push (this|it|to (origin|main|master)))",
      "skill": "pre-push-auto-fix",
      "hint": "Run quality checks and auto-fix before any push (max 3 iterations)."
    },
    {
      "id": "knowledge-graph",
      "pattern": "\\bknowledge graph\\b|\\bgraphify\\b",
      "skill": "graphify",
      "hint": "Build/update the knowledge graph via /graphify. For a mechanical call-graph or symbol lookup only (who calls X), use tokensave instead."
    },
    {
      "id": "stuck-escalation",
      "pattern": "(still (broken|failing|not working)|tried .{0,20}(times|again)|keeps (failing|breaking|crashing)|can.?t (fix|figure)|(3rd|4th|third|fourth) (time|attempt)|no luck)",
      "skill": "goal-iteration",
      "hint": "Repeated failures detected. Invoke superpowers:systematic-debugging if it has not run; after three failed fixes it stops and questions the architecture rather than trying a fourth. If the work has a checkable finish line, set /goal with a turn clause. Never start an unattended loop without user confirmation."
    },
    {
      "id": "tdd",
      "pattern": "\\b(tdd|test[- ]driven|test[- ]first|with (unit )?tests|test coverage)\\b",
      "skill": "goal-iteration",
      "hint": "Invoke superpowers:test-driven-development for the red-green-refactor discipline, then set /goal with a condition the test output proves."
    },
    {
      "id": "bugfix",
      "pattern": "\\b(bug|broken|not working|failing|fails|crash(es|ing)?|regression|stack ?trace)\\b",
      "skill": "goal-iteration",
      "hint": "Invoke superpowers:systematic-debugging: root cause before any fix, one hypothesis at a time."
    },
    {
      "id": "feature",
      "pattern": "\\b(build|create|implement|add|new)\\b.{0,60}\\bfeature\\b|\\bfeature\\b.{0,30}\\b(build|implement)\\b",
      "skill": "goal-iteration",
      "hint": "Invoke superpowers:brainstorming for design and approval first, then implement test-first. Do not write code before the human approves the design."
    },
    {
      "id": "docs",
      "pattern": "\\b(documentation|readme|changelog|api docs|update (the )?docs|document (this|the))\\b",
      "skill": "document-with-goal",
      "hint": "Generate and verify docs until complete (max 5 iterations, 15m)."
    },
    {
      "id": "codebase-review-agentic",
      "pattern": "\\b(review|evaluate|assess)\\b.{0,50}\\b(codebase|repo(sitory)?|code base)\\b",
      "skill": "fullreview",
      "hint": "Run /fullreview. If the product under review is itself agentic (LLM SDK calls, agent loops, MCP, tool schemas, prompt files), fullreview adds domain 11 (twelve-primitive agentic harness review, scored HML 0-5 against the Agent Harness Maturity Specification) automatically."
    },
    {
      "id": "ponytail-review",
      "pattern": "(review for over.engineer|what can (we|i) delete|is this over.engineer|simplify review|ponytail.?review|too much abstraction|find (bloat|dead code))",
      "skill": "ponytail-review",
      "hint": "Over-engineering only: finds what to delete, stdlib replacements, YAGNI violations. One line per finding, net LOC saved at end."
    },
    {
      "id": "ponytail-audit",
      "pattern": "(audit (codebase|for bloat|for over.engineer)|ponytail.?audit|what can (i|we) delete from (repo|codebase)|find bloat in)",
      "skill": "ponytail-audit",
      "hint": "Whole-repo complexity scan ranked by cut size. Lists findings only, does not apply them."
    },
    {
      "id": "prompt-injection-defense",
      "pattern": "\\b(prompt injection|chat endpoint|llm prompt|system prompt (leak|exfil)|untrusted (input|text) (to|into) (the )?(llm|model)|agent tool result)\\b",
      "skill": "prompt-injection-defense",
      "hint": "Pick a threat tier (single-user/internal/public) first, then apply the zero-cost baseline: sanitize, regex-flag, canary+delimiter, output validation. Don't add ML scanners/human-approval queues unless the tier calls for it."
    },
    {
      "id": "ingest-scrubbing",
      "pattern": "\\b(ingest(ion)? pipeline|document ingestion|embed(ding)? documents?|vector store|rag pipeline|knowledge base upload)\\b",
      "skill": "ingest-scrubbing",
      "hint": "Scrub at ingest AND re-check at retrieval \u2014 poisoned corpus entries fire on someone else's unrelated query later. Reuses prompt-injection-defense's baseline on retrieved chunks."
    }
  ]
}
