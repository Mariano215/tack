{
  "version": "1.0.0",
  "description": "Centralized safety limits for all /goal integrations",
  "defaultSafetyLimits": {
    "maxIterations": 10,
    "timeout": "20m",
    "warnAtIteration": 7,
    "failFast": true
  },
  "escalation": {
    "offerAtIteration": 4,
    "options": [
      "switch strategy (new hypothesis, different approach)",
      "hand off to built-in /loop (self-paced retries; REQUIRES explicit user confirmation; token-expensive)",
      "stop and report findings"
    ],
    "description": "At iteration 4 without convergence (or at the skill's own limit when lower), pause and present these options to the user. Never auto-start /loop. Hard limits below still apply and override everything."
  },
  "skillSafetyLimits": {
    "tdd-with-goal": {
      "maxIterations": 10,
      "timeout": "20m",
      "description": "Most features should pass tests within 10 attempts"
    },
    "debug-with-goal": {
      "maxIterations": 15,
      "timeout": "30m",
      "description": "Bugs may need more hypothesis iterations"
    },
    "build-with-goal": {
      "maxIterations": 25,
      "timeout": "60m",
      "description": "Full feature pipeline takes longer"
    },
    "pre-push-auto-fix": {
      "maxIterations": 3,
      "timeout": "10m",
      "description": "Fail fast before push - conservative limits"
    },
    "document-with-goal": {
      "maxIterations": 5,
      "timeout": "15m",
      "description": "Doc generation should converge quickly"
    }
  },
  "emergencyKillSwitch": {
    "enabled": true,
    "globalMaxIterations": 30,
    "globalTimeout": "90m",
    "description": "Hard limits that override all skill configurations"
  }
}
